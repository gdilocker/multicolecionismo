# =============================================================================
# SUPABASE CONFIGURATION (REQUIRED)
# =============================================================================
VITE_SUPABASE_URL=your_supabase_project_url
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key
VITE_SUPABASE_FUNCTIONS_URL=your_supabase_project_url/functions/v1

# =============================================================================
# DEVELOPMENT MODE (LOCAL TESTING)
# =============================================================================
# Set to 'true' to enable development mode features
VITE_DEV_MODE=false

# Mock Payment Mode - Bypass PayPal for testing
# Set to 'true' to test full sales funnel without real PayPal transactions
VITE_USE_PAYMENT_MOCK=true

# =============================================================================
# PAYPAL CONFIGURATION (REQUIRED FOR PRODUCTION)
# =============================================================================
# Obtain from: https://developer.paypal.com/dashboard/applications
# Create a Business account and REST API app
VITE_PAYPAL_CLIENT_ID=your_paypal_client_id
VITE_PAYPAL_SECRET=your_paypal_secret

# Webhook ID - Configure at: https://developer.paypal.com/dashboard/webhooks
# Point to: https://your-supabase-url.supabase.co/functions/v1/paypal-webhook
VITE_PAYPAL_WEBHOOK_ID=your_paypal_webhook_id

# =============================================================================
# EMAIL SERVICE (REQUIRED FOR PRODUCTION)
# =============================================================================
# Resend API Key (RECOMMENDED - Free tier: 3,000 emails/month)
# Obtain from: https://resend.com/api-keys
VITE_RESEND_API_KEY=your_resend_api_key

# =============================================================================
# CLOUDFLARE TURNSTILE (ANTI-BOT PROTECTION)
# =============================================================================
# Obtain from: https://dash.cloudflare.com/?to=/:account/turnstile
VITE_TURNSTILE_SITE_KEY=your_turnstile_site_key
VITE_TURNSTILE_SECRET_KEY=your_turnstile_secret_key

# =============================================================================
# GOOGLE SAFE BROWSING (LINK SECURITY)
# =============================================================================
# Obtain from: https://console.cloud.google.com/apis/credentials
# Enable Safe Browsing API first
VITE_GOOGLE_SAFE_BROWSING_API_KEY=your_google_safe_browsing_api_key

# =============================================================================
# NOTES:
# =============================================================================
# 1. NEVER commit the .env file with real credentials
# 2. Copy this file to .env and fill in your actual values
# 3. For Netlify/Vercel deployment, add these as environment variables
# 4. Supabase Edge Functions need secrets added via Dashboard → Edge Functions → Secrets
# 5. System uses internal subdomain licensing (.com.rich) - NO external domain registrar needed
